{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["AGENTS.md"],
  "command": {
    "dev": {
      "description": "Start the Next.js dev server",
      "template": "Run npm run dev in the project root"
    },
    "build": {
      "description": "Build the project for production",
      "template": "Run npm run build in the project root"
    },
    "start": {
      "description": "Start the production server",
      "template": "Run npm start in the project root"
    },
    "lint": {
      "description": "Run Next.js lint",
      "template": "Run npm run lint in the project root"
    }
  }
}
